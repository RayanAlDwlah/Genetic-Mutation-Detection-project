# Human-readable floor requirements. CI and Docker install from
# requirements-lock.txt for exact reproducibility; this file is for
# developers installing into fresh environments.

# Data handling
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14                      # parquet IO
scipy>=1.11

# ML
scikit-learn>=1.3,<2
xgboost>=2.0,<4
optuna>=4.0                      # TPE hyperparameter search
shap>=0.42                       # TreeSHAP explainability

# Deep learning (Phase 2)
torch>=2.0
# Note: transformers 5.x requires Python 3.10+ and liblzma at runtime.
transformers>=4.40

# Plotting / reporting
matplotlib>=3.7
seaborn>=0.12

# Utilities
requests>=2.28
pyyaml>=6.0
tqdm>=4.65

# Interactive / optional
jupyter
ipykernel
notebook

# Test + lint (dev)
pytest>=7.4
pytest-cov>=4.1
pytest-xdist>=3.5
mypy>=1.8
ruff>=0.3
black>=24.0
pre-commit>=3.6
pip-audit>=2.7

# Structural features (Stage 2.2)
biopython>=1.83
freesasa>=2.2
# NOTE: DSSP is installed as a system binary (apt: dssp / brew: dssp);
# biopython wraps it at runtime.

# Demo (Stage 4)
streamlit>=1.31
py3Dmol>=2.0
